<Window
    x:Class="CornerstoneQueue.MainWindow"
    x:Name="AppWindowRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Cornerstone 样品队列">

  <Grid x:Name="DockRoot" Padding="8,6" RowSpacing="4">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="6">
      <Ellipse x:Name="DotBridge" Width="8" Height="8" VerticalAlignment="Center" Fill="Gray" />
      <TextBlock
        x:Name="TxtStatusLine"
        VerticalAlignment="Center"
        FontSize="11"
        TextTrimming="CharacterEllipsis"
        TextWrapping="NoWrap"
        Text="…" />
    </StackPanel>

    <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="6">
      <Button
        x:Name="BtnRefresh"
        Padding="8,4"
        FontSize="12"
        Content="刷新"
        Click="OnRefreshClick" />
      <Button
        x:Name="BtnSettings"
        Padding="8,4"
        FontSize="12"
        Content="设置"
        Click="OnSettingsClick" />
      <Button
        x:Name="BtnSend"
        Padding="8,4"
        FontSize="12"
        Content="发送"
        Style="{StaticResource AccentButtonStyle}"
        Click="OnSendClick" />
    </StackPanel>

    <Grid Grid.Row="2" x:Name="QueuePanel">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <Grid
        x:Name="QueueColumnHeader"
        Grid.Row="0"
        Height="24"
        Margin="0,0,0,2"
        ColumnSpacing="0"
        Visibility="Collapsed">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="{Binding ElementName=AppWindowRoot, Path=QueueNameColumnLength}" />
          <ColumnDefinition Width="12" />
          <ColumnDefinition Width="{Binding ElementName=AppWindowRoot, Path=QueueDescriptionColumnLength}" />
        </Grid.ColumnDefinitions>
        <Border
          Grid.Column="0"
          Padding="4,0"
          Background="{ThemeResource LayerFillColorDefaultBrush}"
          BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
          BorderThickness="0,0,0,1">
          <TextBlock
            VerticalAlignment="Center"
            FontSize="10"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Text="名称" />
        </Border>
        <Border
          x:Name="HeaderColumnSplitter"
          Grid.Column="1"
          HorizontalAlignment="Stretch"
          Background="{ThemeResource AccentFillColorDefaultBrush}"
          Opacity="0.7"
          PointerPressed="OnHeaderColumnSplitterPointerPressed"
          PointerMoved="OnHeaderColumnSplitterPointerMoved"
          PointerReleased="OnHeaderColumnSplitterPointerReleased"
          PointerCanceled="OnHeaderColumnSplitterPointerReleased"
          DoubleTapped="OnHeaderColumnSplitterDoubleTapped" />
        <Border
          Grid.Column="2"
          Padding="4,0"
          Background="{ThemeResource LayerFillColorDefaultBrush}"
          BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
          BorderThickness="0,0,0,1">
          <TextBlock
            VerticalAlignment="Center"
            FontSize="10"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Text="说明" />
        </Border>
      </Grid>

      <ListView
        Grid.Row="1"
        x:Name="QueueList"
        MinHeight="72"
        MaxHeight="280"
        FontFamily="{StaticResource QueueMonoFont}"
        SelectionMode="Extended"
        IsItemClickEnabled="True"
        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
        ScrollViewer.HorizontalScrollMode="Disabled"
        SizeChanged="OnQueueListSizeChanged"
        DoubleTapped="OnQueueListDoubleTapped">
      <ListView.ItemContainerStyle>
        <Style TargetType="ListViewItem">
          <Setter Property="MinHeight" Value="24" />
          <Setter Property="Padding" Value="4,2" />
          <Setter Property="HorizontalContentAlignment" Value="Stretch" />
          <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="CommonStates">
              <VisualState x:Name="Normal" />
              <VisualState x:Name="PointerOver">
                <Storyboard>
                  <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Background">
                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleFillColorSecondaryBrush}" />
                  </ObjectAnimationUsingKeyFrames>
                </Storyboard>
              </VisualState>
              <VisualState x:Name="Pressed">
                <Storyboard>
                  <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Background">
                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleFillColorTertiaryBrush}" />
                  </ObjectAnimationUsingKeyFrames>
                </Storyboard>
              </VisualState>
              <VisualState x:Name="Selected">
                <Storyboard>
                  <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Background">
                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AccentFillColorDefaultBrush}" />
                  </ObjectAnimationUsingKeyFrames>
                  <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                    <DiscreteObjectKeyFrame KeyTime="0" Value="White" />
                  </ObjectAnimationUsingKeyFrames>
                </Storyboard>
              </VisualState>
              <VisualState x:Name="PointerOverSelected">
                <Storyboard>
                  <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Background">
                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AccentFillColorSecondaryBrush}" />
                  </ObjectAnimationUsingKeyFrames>
                  <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                    <DiscreteObjectKeyFrame KeyTime="0" Value="White" />
                  </ObjectAnimationUsingKeyFrames>
                </Storyboard>
              </VisualState>
              <VisualState x:Name="PressedSelected">
                <Storyboard>
                  <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Background">
                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AccentFillColorTertiaryBrush}" />
                  </ObjectAnimationUsingKeyFrames>
                  <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                    <DiscreteObjectKeyFrame KeyTime="0" Value="White" />
                  </ObjectAnimationUsingKeyFrames>
                </Storyboard>
              </VisualState>
            </VisualStateGroup>
            <VisualStateGroup x:Name="FocusStates">
              <VisualState x:Name="Focused" />
              <VisualState x:Name="Unfocused" />
              <VisualState x:Name="PointerFocused" />
            </VisualStateGroup>
          </VisualStateManager.VisualStateGroups>
        </Style>
      </ListView.ItemContainerStyle>
      <ListView.ItemTemplate>
        <DataTemplate>
          <Grid ColumnSpacing="0">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="{Binding ElementName=AppWindowRoot, Path=QueueNameColumnLength}" />
              <ColumnDefinition Width="12" />
              <ColumnDefinition Width="{Binding ElementName=AppWindowRoot, Path=QueueDescriptionColumnLength}" />
            </Grid.ColumnDefinitions>
            <TextBlock
              Grid.Column="0"
              Margin="4,0,0,0"
              FontFamily="{StaticResource QueueMonoFont}"
              FontSize="{Binding ElementName=AppWindowRoot, Path=QueueListFontSize}"
              Text="{Binding SampleNameDisplay}"
              TextTrimming="CharacterEllipsis"
              TextWrapping="NoWrap" />
            <TextBlock
              Grid.Column="2"
              Margin="4,0,0,0"
              FontFamily="{StaticResource QueueMonoFont}"
              FontSize="{Binding ElementName=AppWindowRoot, Path=QueueListFontSize}"
              Text="{Binding SampleDescriptionDisplay}"
              TextTrimming="CharacterEllipsis"
              TextWrapping="NoWrap" />
          </Grid>
        </DataTemplate>
      </ListView.ItemTemplate>
      </ListView>
    </Grid>

    <TextBlock
      Grid.Row="3"
      x:Name="TxtResultLine"
      FontSize="11"
      Foreground="{ThemeResource TextFillColorSecondaryBrush}"
      TextTrimming="CharacterEllipsis"
      TextWrapping="NoWrap"
      Visibility="Collapsed" />
  </Grid>
</Window>
