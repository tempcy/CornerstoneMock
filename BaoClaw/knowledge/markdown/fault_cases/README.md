# 故障案例（fault_cases）— 检索摘录

结构化 JSON 源文件见 [`../../raw/fault_cases/`](../../raw/fault_cases/README.md)。

当前示例案例（`verified`）：

| case_id | 标题 |
|---------|------|
| `fc-gc8-sulfur-tail-oxygun-001` | CS844 硫拖尾 / 氧枪流量不足 |
| `fc-gc8-autoloader-stall-001` | CS844 自动进样机械手卡死 |
| `fc-go6-furnacehead-leak-001` | ON(H)836 炉头漏气 / 动力气压力 |

完整字段与诊断步骤以 `raw/fault_cases/examples/*.json` 为准；本目录后续存放自动生成的 `.md` 摘要。
